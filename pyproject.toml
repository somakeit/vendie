[project]
name = "vendie"
version = "0.1.0"
description = ""
authors = [{ name = "Sam Cork", email = "sam57719@gmail.com" }]
requires-python = ">=3.11.0"
dependencies = [
    "python-decouple>=3.8",
    "pyserial>=3.5"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
